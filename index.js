function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  square(30, 20, 55);
}
//function keyPressed() {
  if (key === 'a') {
    // Code to run.
  }
  if (key === 's') {
    // Code to run.
  }
  if (key === 'd') {
    // Code to run.
  }
  if (key === 'w') {
    // Code to run.
  }
  if (keyCode === 'q') {
    // Code to run. specal attack 1
  }
  if (keyCode === 'e') {
    // Code to run.
  }
  if (keyCode === '1') {
    // Code to run. super attack
  }
  if (keyCode === 'arrowleft') {
    // Code to run.
  }
  if (keyCode === 'arrowright') {
    // Code to run.
  }
  if (keyCode === 'arrowdown') {
    // Code to run.
  }
  if (keyCode === 'arrowup') {
    // Code to run.
  }
//}
